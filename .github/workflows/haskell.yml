name: Release Executable

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Haskell
        uses: haskell/actions/setup@v1
        with:
          ghc-version: 9.2.5
          cabal-version: '3.6.2.0'

      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build executable
        run: |
          cabal build
          EXECUTABLE_PATH=$(cabal v2-exec which privgen-exe)
          echo "::set-output name=executable_path::$EXECUTABLE_PATH"
          
      - name: Create release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          body: |
            Release ${{ github.ref }}
          draft: false
          prerelease: false

      - name: Upload executable
        uses: actions/upload-artifact@v2
        with:
          name: privgen
          path: ${{ steps.create_release.outputs.executable_path }}
